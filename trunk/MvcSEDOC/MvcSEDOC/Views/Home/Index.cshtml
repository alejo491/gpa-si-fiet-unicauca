@model MvcSEDOC.Models.usuario
@{
    ViewBag.Title = "Inicio de Sesión SEDOC";
    Layout = "~/Views/Shared/_WorkLayout.cshtml";
}

@if(Request.IsAuthenticated) {
    Response.Redirect(Url.Action("Logout","User"));
}

<h2>Inicio</h2>

El Sistema de Evaluación Docente SEDOC permite llevar el registro de las evaluaciones de los docentes en las diferentes labores que estos realizan.

@if (ViewBag.StartScriptResult != "")
{
    <br />
    <br />
    <span style="color: Red;">@ViewBag.StartScriptResult</span>
}

@section LoginFormSection
{
    <div class="sidebarheader">Inicio de Sesión</div>
	<div class="content">
        @if (!string.IsNullOrEmpty(ViewBag.Error))
        {
        <div class="error">@ViewBag.Error</div>
        }
		<form method="post" action="@Url.Action("Login", "User")">
            <label for="login">Usuario</label><br />
            <input type="text" name="emailinstitucional" value="@(Model!=null ? Model.emailinstitucional : string.Empty)" /><br />
            <label for="password">Contraseña</label><br />
            <input type="password" name="password" /><br />
            <input type="submit" value="Ingresar"/>
        </form>
	</div>
}
